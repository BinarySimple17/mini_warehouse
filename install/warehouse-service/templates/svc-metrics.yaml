{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "warehouse-service.fullname" . }}-metrics
  labels:
    {{- include "warehouse-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: metrics
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http-metrics
  selector:
    {{- include "warehouse-service.selectorLabels" . | nindent 4 }}
{{- end }}